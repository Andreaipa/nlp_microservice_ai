# Dependencias de ENTRENAMIENTO. No deben instalarse en producción.
#
# Nota: ultralytics arrastra opencv-python, que convive con la variante
# headless de base.txt. En una estación de trabajo con escritorio no da
# problemas; por eso esta pila no se instala en el contenedor ni en CI.
-r base.txt
ultralytics==8.4.157
torch>=2.4
torchvision>=0.19
onnx>=1.17
onnxslim>=0.1.48
# Necesario para torch.onnx.export en torch >= 2.6
onnxscript>=0.1
matplotlib>=3.9
pandas>=2.2
